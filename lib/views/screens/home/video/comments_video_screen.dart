import 'package:flutter/material.dart';

class CommentsVideoScreen extends StatelessWidget {
  const CommentsVideoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}